# Tiny Tapeout project information
project:
  title:        "4 registers"      # Project title
  author:       "Oliver and Gabriel"      # Your name
  discord:      ""      # Your discord username, for communication and automatically assigning you a Tapeout role (optional)
  description:  "4 adressable really cool registers"      # One line description of what your project does
  language:     "VHDL"  # other examples include Verilog, SystemVerilog, Amaranth, etc
  clock_hz:     0       # Clock frequency in Hz (or 0 if not applicable)

  # How many tiles your design occupies? A single tile is about 167x108 uM.
  tiles: "1x1"          # Valid values: 1x1, 1x2, 2x2, 3x2, 4x2, 6x2 or 8x2

  # Your top module name must start with "tt_um_". Make it unique by including your github username:
  top_module:  "tt_um_registers_circ"

  # List your project's source files here.
  # Source files must be in ./src and you must list each source file separately, one per line.
  # Don't forget to also update `PROJECT_SOURCES` in test/Makefile.
  #
  # WARNING: VHDL support in Tiny Tapeout is experimental. The template may get changes during
  #          the shuttle and VHDL support may be dropped for future shuttles if we see low usage.
  source_files:
    - "project.vhdl"

# The pinout of your project. Leave unused pins blank. DO NOT delete or add any pins.
# This section is for the datasheet/website. Use descriptive names (e.g., RX, TX, MOSI, SCL, SEG_A, etc.).
pinout:
  # Inputs
  ui[0]: "data"
  ui[1]: "data"
  ui[2]: "data"
  ui[3]: "data"
  ui[4]: "data"
  ui[5]: "data"
  ui[6]: "data"
  ui[7]: "data"

  # Outputs
  uo[0]: "reg data"
  uo[1]: "reg data"
  uo[2]: "reg data"
  uo[3]: "reg data"
  uo[4]: "reg data"
  uo[5]: "reg data"
  uo[6]: "reg data"
  uo[7]: "reg data"

  # Bidirectional pins
  uio[0]: "write"
  uio[1]: "address 0"
  uio[2]: "address 1"
  uio[3]: ""
  uio[4]: ""
  uio[5]: ""
  uio[6]: ""
  uio[7]: ""

# Do not change!
yaml_version: 6
